I have completed the requested audit. Here is a summary of the actions taken:

1. **Hygiene & File Whitelisting:** 
   - Scanned `/root` and identified that `patch_ideas.py` was missing from `cleanup_system.py`'s whitelist. 
   - Updated the `WHITELIST` in `cleanup_system.py` to prevent accidental deletion.

2. **Dashboard UI Consistency:**
   - Modified `dashboard.py` to ensure it only pulls and displays the indicators actively generated by the live bot. Specifically, I removed the `alt_24h_ret` indicator which is no longer exported by `bot.py`'s `self.current_indicators`, leaving only the accurate `alt_4h_ret` and others.
   - Reviewed `forecast_dashboard.py`, `view_blacklist.py`, `backtest_dashboard.py`, and `bolero.py` and found them fully aligned without syntax errors or outdated variables.

3. **Strategy Naming Alignment:**
   - Updated `ai_manager.py` to correctly reference the new **Volatile Momentum & Decoupling Squeeze** strategy in its AI prompt, removing the outdated reference to *Trend-Filtered BB Squeeze Breakout*.

4. **Bot vs. Backtester:**
   - Ran `check_consistency.py` and verified complete alignment between `bot.py` and `portfolio_backtester.py`. Both are correctly using Strategy V159 logic.

I have generated the clean summary report at `/root/coherence_report.md` as requested. Please let me know if you need any further modifications!
 thresholds, parameters, portfolio guards, and versioning (V159) are fully coherent.

The system's logic and data pipelines across the live bot, backtester, dashboards, and AI manager are now aligned.
